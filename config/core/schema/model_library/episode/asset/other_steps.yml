# the type of dynamic content
type: "static"

# the shotgun field to use for the folder name
name: "other_steps"

filters:
    - { "path": "short_name", "relation": "not_in", "values": [ "UID", "MDL", "UUW", "REF" ] }